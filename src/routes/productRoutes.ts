// import { Router } from 'express';
// import { authenticate } from '../middleware/auth';
// import { checkRole } from '../middleware/roleCheck';
// import { addProduct, updateProduct, deleteProduct, getAllProducts } from '../controllers/productController';

// const router = Router();

// router.post('/', authenticate, checkRole(['Admin']), addProduct);
// router.put('/:productId', authenticate, checkRole(['Admin']), updateProduct);
// router.delete('/:productId', authenticate, checkRole(['Admin']), deleteProduct);
// router.get('/', authenticate, getAllProducts);

// export default router;
